<div class="container mt-5" style="background-color: black; opacity: .8;">
  <div class="p-3">
    <h3>{{terms.title}}</h3>
    <h5><small>Updated : {{terms.updated}}</small></h5>
    {{#each terms.sections}}
    <h4>{{title}}</h4>
    {{#each paragraphs}}
    <p>{{ . }}</p>
    {{/each}}
    {{/each}}
  </div>
</div>